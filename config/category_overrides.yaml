version: 1
overrides:
  # Template starter override.
  # Copy this file to your runtime config path and replace examples with
  # normalized fingerprints from your own data.
  - fingerprint: paypal payment
    category: Transfers
    subcategory: Wallet Transfer
    bank: null
    account_label: null
    hit_count: 0
